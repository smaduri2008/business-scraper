[phases.setup]
nixPkgs = ['python311']
aptPkgs = ['chromium', 'chromium-driver']

[phases.install]
cmds = [
  'pip install --upgrade pip',
  'pip install -r requirements.txt',
  'PLAYWRIGHT_BROWSERS_PATH=/root/.cache/ms-playwright playwright install --with-deps chromium'
]

[start]
cmd = 'gunicorn run:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1'[phases.setup]
nixPkgs = ['python311']

[phases.install]
cmds = [
  'pip install --upgrade pip',
  'pip install -r requirements.txt',
  'playwright install-deps',
  'playwright install chromium'
]

[start]
cmd = 'gunicorn run:app --bind 0.0.0.0:$PORT --timeout 300 --w[phases.setup]
nixPkgs = ['python311']

[phases.install]
cmds = [
  'apt-get update -y',
  'apt-get install -y wget gnupg ca-certificates procps libxss1',
  'apt-get install -y fonts-liberation libasound2 libatk-bridge2.0-0 libatk1.0-0 libatspi2.0-0 libcairo2 libcups2 libdbus-1-3 libdrm2 libgbm1 libglib2.0-0 libgtk-3-0 libnspr4 libnss3 libpango-1.0-0 libx11-6 libxcb1 libxcomposite1 libxdamage1 libxext6 libxfixes3 libxkbcommon0 libxrandr2 xdg-utils',
  'pip install --upgrade pip',
  'pip install -r requirements.txt',
  'playwright install chromium',
  'playwright install-deps'
]

[start]
cmd = 'gunicorn run:app --bind 0.0.0.0:$PORT --timeout 600 --workers 1 --log-level info'
